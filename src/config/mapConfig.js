// Google Maps API configuration

// Set this to your Google Maps API key
export const GOOGLE_MAPS_API_KEY = '';

// Flag to indicate if Google Maps should be enabled by default
// Set to false until you have a valid API key
export const ENABLE_MAPS_BY_DEFAULT = false;

// Default map center (Addis Ababa, Ethiopia)
export const DEFAULT_MAP_CENTER = {
  lat: 9.0222,
  lng: 38.7468
};

// Map libraries to load
export const MAP_LIBRARIES = ['places'];
